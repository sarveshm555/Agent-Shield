# Server Configuration
PORT=5000

# MongoDB Atlas Connection String
# Example: mongodb+srv://<username>:<password>@cluster0.mongodb.net/agentshield?retryWrites=true&w=majority
MONGODB_URI=

# AI Provider Keys (At least one recommended for AI security evaluations, fallback to heuristic rules if omitted)
OPENAI_API_KEY=
GEMINI_API_KEY=
